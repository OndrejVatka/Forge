## What does this change?

<!-- Brief description, and link the issue if there is one. -->

## Why?

<!-- What problem does it solve? -->

## Checks

- [ ] `npm run typecheck` passes
- [ ] `npm run lint` passes
- [ ] `npm test` passes
- [ ] `npm run format` run

## If this touches `db/schema.sql`

- [ ] Access-control impact described above
- [ ] Migration added under `db/migrations/` for existing installs
- [ ] Tested against a real Supabase project
