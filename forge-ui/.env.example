# Forge web UI environment (Vite inlines VITE_* at build time).
# Copy to `.env` for local dev. Safe to expose in the client (anon key + URL).

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
